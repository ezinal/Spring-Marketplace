spring:
    datasource:
        url: jdbc:mysql://localhost:3306/jss_final_prj
        username: emre
        password: password
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                show_sql: true
                format_sql: true
                globally_quoted_identifiers: true
    sql:
        init:
            mode: always
            data-locations: classpath:data.sql